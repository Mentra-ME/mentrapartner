import React from 'react';

export const DirhamIcon: React.FC<React.SVGProps<SVGSVGElement>> = ({className}) => (
    <span className={`inline-block text-[18px] font-bold text-current leading-none ${className}`}>
    د.إ
    </span>
);
